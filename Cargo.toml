[package]
name = "rocblas-lite"
version = "0.1.0"
edition = "2021"
description = "Lightweight safe Rust bindings for AMD rocBLAS"
license = "MIT"

[dependencies]
libc = "0.2"
thiserror = "1.0"
num-traits = "0.2"

[build-dependencies]
bindgen = "0.69"
pkg-config = "0.3"

[dev-dependencies]
rand = "0.8"
approx = "0.5"
